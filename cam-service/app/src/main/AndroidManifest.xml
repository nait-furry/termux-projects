<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.CameraForegroundService"
            android:exported="true"
            android:foregroundServiceType="camera|microphone" />

        <receiver
            android:name=".service.CameraCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.termux.camera.START" />
                <action android:name="com.termux.camera.STOP" />
                <action android:name="com.termux.camera.FRONT" />
                <action android:name="com.termux.camera.BACK" />
                <action android:name="com.termux.camera.SWITCH" />
                <action android:name="com.termux.camera.CAPTURE" />
                <action android:name="com.termux.camera.BURST" />
                <action android:name="com.termux.camera.START_VIDEO" />
                <action android:name="com.termux.camera.STOP_VIDEO" />
                <action android:name="com.termux.camera.SET_RATE" />
                <action android:name="com.termux.camera.SET_MODE" />
                <action android:name="com.termux.camera.SET_RESOLUTION" />
            </intent-filter>
        </receiver>

        <service
            android:name=".tiles.CameraTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_camera"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
    </application>
</manifest>
